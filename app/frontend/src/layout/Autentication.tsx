import { LoginUser } from "../pages/LoginUser"

export const Atutentication = () => {
    return (
        <section className="bg-slate-900 bg-opacity-60 flex w-1/2 h-screen">
            <LoginUser/>
            <div className="flex w-1/2 h-screen">oi</div>
        </section>
    )
}